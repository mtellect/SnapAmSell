class empty {
  /*Center pages() {
    return Center(
      child: Padding(
        padding: const EdgeInsets.all(20.0),
        child: Column(
          mainAxisSize: MainAxisSize.min,
          children: <Widget>[
            position == 0
                ? Image.asset(
                    icons[position],
                    height: 80,
                    width: 80,
                  )
                : Image.asset(
                    icons[position],
                    color: white,
                    height: position < 4 ? 150 : 100,
                    width: position < 4 ? 150 : 100,
                  ),
            SizedBox(
              height: 15,
            ),
            Text(
              text1[position],
              textAlign: TextAlign.center,
              style: TextStyle(
                  fontWeight: FontWeight.normal,
                  fontFamily: "NirmalaB",
                  fontSize: 22,
                  color: position == 0 ? black : white,
                  decoration: TextDecoration.none),
            ),
            Text(
              text2[position],
              textAlign: TextAlign.center,
              style: TextStyle(
                  fontWeight: FontWeight.normal,
                  fontFamily: "Nirmala",
                  fontSize: 17,
                  color: (position == 0 ? black : white).withOpacity(.5),
                  decoration: TextDecoration.none),
            ),
          ],
        ),
      ),
    );
  }*/
}
